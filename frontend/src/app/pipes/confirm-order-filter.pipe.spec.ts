import { ConfirmOrderFilterPipe } from './confirm-order-filter.pipe';

describe('ConfirmOrderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfirmOrderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
