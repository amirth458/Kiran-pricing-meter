import { FeatureLockDirective } from './feature-lock.directive';

describe('FeatureLockDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureLockDirective();
    expect(directive).toBeTruthy();
  });
});
